import styled from 'styled-components';

const Img = styled.img`
	position: absolute;
	max-height: 98%;
	max-width: 98%;
	top: 50%;
	left: 50%;
	transform: translate(-50%, -50%);
`;

export default Img;